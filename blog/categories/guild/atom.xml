<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: guild | 唔好巨集]]></title>
  <link href="http://openkorer.github.com/blog/categories/guild/atom.xml" rel="self"/>
  <link href="http://openkorer.github.com/"/>
  <updated>2013-03-25T20:19:04+08:00</updated>
  <id>http://openkorer.github.com/</id>
  <author>
    <name><![CDATA[openkorer]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[指令同步]]></title>
    <link href="http://openkorer.github.com/blog/2013/03/25/sync/"/>
    <updated>2013-03-25T07:06:00+08:00</updated>
    <id>http://openkorer.github.com/blog/2013/03/25/sync</id>
    <content type="html"><![CDATA[<p>以下是利用隊伍頻道送出同步指令的巨集</p>

<p>``` perl sync
automacro guildcmd {</p>

<pre><code>guild /cmd (.*)/
exclusive 1
call {
$do = $.lastMatch1
do $do
}
</code></pre>

<p>}
```</p>

<p>好處:</p>

<ul>
<li>只要同一隊伍即可。</li>
</ul>


<p>壞處:</p>

<ul>
<li>最多支持12人。</li>
<li>隊伍中的人也會看到指令，還可以對你的外掛下達任何指令。</li>
</ul>


<blockquote><h2>以下是利用公會頻道送出同步指令的巨集</h2>

<h3>好處:</h3>

<ul>
<li>不需要同一隊伍，只需同一公會即可。</li>
<li>最多支持50人，看擴充組合體制等級而定。</li>
</ul>
</blockquote>

<h3>壞處:</h3>

<ul>
<li>同公會的人也會看到指令，還可以對你的外掛下達任何指令。</li>
</ul>


<p>``` perl sync
automacro partycmd {</p>

<pre><code>party /cmd (.*)/
exclusive 1
call {
$do = $.lastMatch1
do $do
}
</code></pre>

<p>}
```</p>

<blockquote><h2>This is a header.</h2>

<ul>
<li>This is the first list item.</li>
<li>This is the second list item.</li>
</ul>


<p>Here's some example code:</p>

<pre><code>return shell_exec("echo $input | $markdown_script");
</code></pre></blockquote>

<pre><code>    do move prontera
do move prontera
</code></pre>

<p>I strongly recommend against using any <code>&lt;blink&gt;</code> tags.</p>

<p>I wish SmartyPants used named entities like <code>&amp;mdash;</code>
instead of decimal-encoded entites like <code>&amp;#8212;</code>.</p>
]]></content>
  </entry>
  
</feed>
